# dependencies
node_modules/
**/node_modules/

# local toolchain (self-contained node install)
.node/

# build output
dist/
**/dist/
.next/
out/

# test output
coverage/
.vitest/

# editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# env / secrets
.env
.env.*
!.env.example

# ad-hoc secret/deploy scripts (e.g. `fly secrets set ...` with plaintext keys).
# Keep these OUT of the repo — set secrets via `fly secrets set` directly.
Untitled-*
*secrets*.sh
*-secret*.sh
fly-secrets*.sh
set-secrets*.sh

# wallet keypairs & private keys — NEVER commit (devnet or mainnet)
.keys/
secrets/
wallets/
*.keypair.json
*-keypair.json
*.wallet.json
keypair*.json
id.json
*.pem
*.key
*.privatekey

# solana localnet / anchor artifacts
test-ledger/
.anchor/
ledger/

# logs
*.log
logs/

# vite cache
.vite/

# typescript build cache
*.tsbuildinfo
